## My personal website

The parameters of the website are in the file `_config.yml`.

To serve the website locally, run the command `bundle exec jekyll serve` in the root directory of the website.

To update the website, run the command `bundle update` in the root directory of the website.

